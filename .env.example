# Copy to ".env" for local builds with ./scripts/build_dist.sh
BASE_URL=https://isanto1306.github.io/zima-appstore

# Optional Docker Hub credentials, only to raise anonymous pull limits.
DOCKERHUB_USERNAME=
DOCKERHUB_TOKEN=
